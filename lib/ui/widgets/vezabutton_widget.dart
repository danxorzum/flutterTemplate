part of 'widget.dart';

class VezaButton extends StatelessWidget {
  /// Botton estandar de vezabela
  const VezaButton({Key? key, this.text}) : super(key: key);

  /// Texto del boton
  final String? text;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
