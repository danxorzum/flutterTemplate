part of 'widget.dart';

class VezaTextWidget extends StatelessWidget {
  const VezaTextWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
