import 'package:templeate/ui/widgets/widget.dart';

class LoginView extends StatelessWidget {
  const LoginView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final x = Get.find();
    return Container(
      child: VezaButton(),
    );
  }
}
